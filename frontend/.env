# Backend for Vite proxy — same machine/LAN as HOST:PORT in backend/.env
# Leave VITE_API_URL unset in dev so the browser calls /api/* on the Vite origin (no CORS).
VITE_PROXY_TARGET=http://192.168.88.33:6001